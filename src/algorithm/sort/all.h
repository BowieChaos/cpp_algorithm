#include "rotate_array.h"
#include "inverse_pair.h"
#include <iostream>
template<class T>
void print_vector(std::vector<T>& vec){
    for (auto &v : vec)
    {
        std::cout<<v<<" "<<std::endl;
    }
    
}